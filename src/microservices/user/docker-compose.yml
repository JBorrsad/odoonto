# User microservice - standalone
version: '3.9'

services:
  user-svc:
    build: .
    container_name: user-microservice-standalone
    ports:
      - 3001:3000
    environment:
      - NODE_ENV=production
      - RMQ_URL=${RMQ_URL:-amqp://localhost:5672}
      - DB_HOST=${DB_HOST:-localhost}
      - DB_PORT=${DB_PORT:-5432}
      - DB_USERNAME=${DB_USERNAME:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-postgres}
      - DB_DATABASE=${DB_DATABASE:-odoonto}
    networks:
      - user-network

networks:
  user-network:
    driver: bridge
